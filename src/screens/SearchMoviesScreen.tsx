import React from 'react';
import { SafeAreaView, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
});

const SearchMoviesScreen = () => <SafeAreaView style={styles.container} />;

export default SearchMoviesScreen;
